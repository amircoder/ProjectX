<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alphacoder.ui">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_job_gates"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_job_gates"
        android:supportsRtl="true"
        android:name="com.alphacoder.app.MainApp">
        <activity android:name="com.aba.feature_splash.SplashActivity"
            android:theme="@style/AppTheme.launcherTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>